name: app_review_dialog_example
description: Example app for the app_review_dialog package.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=2.17.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  app_review_dialog:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
